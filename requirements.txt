# ── Core ──────────────────────────────────────────
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
gunicorn>=22,<23

# ── Database (Async PostgreSQL) ───────────────────
SQLAlchemy[asyncio]>=2.0,<3
asyncpg>=0.30,<1
greenlet>=3.0                     # required by SQLAlchemy asyncio

# ── Auth & Security ──────────────────────────────
python-jose[cryptography]>=3.3,<4
passlib[bcrypt]>=1.7,<2
bcrypt==4.0.1                          # pinned for passlib 1.7.x compat
python-multipart>=0.0.18          # OAuth2 form parsing

# ── Validation & Config ─────────────────────────
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
python-dotenv>=1.0,<2

# ── Caching ──────────────────────────────────────
redis>=5.0,<6

# ── Migrations ───────────────────────────────────
alembic>=1.13,<2

# ── Testing ──────────────────────────────────────
pytest>=8,<9
pytest-asyncio>=0.24,<1
httpx>=0.27,<1
